import { R as RailwayClientConfig } from './index-DKbAXI_A.js';
export { B as BucketConfig, a as BucketNode, b as BucketRegion, C as ChangeDeployEffect, c as ChangeDiagnostic, d as ChangeSeverity, e as CompileResult, D as DEFAULT_RAILWAY_GRAPHQL_ENDPOINT, f as DatabaseConfig, g as DatabaseNode, E as EnvironmentConfig, h as EvaluatedRailwayProject, G as GraphCompileOptions, i as GraphIndex, I as IacClient, P as ProjectDefinition, j as ProjectNode, k as RAILWAY_GRAPH_VERSION, l as RailwayChange, m as RailwayChangeSet, n as RailwayGraph, o as RailwayProgram, p as RailwayResourceName, q as RailwayServiceName, r as ResourceAddress, s as ResourceNode, S as ServiceConfigInput, t as ServiceNode, V as VariableValue, u as bucket, v as changeSetToEnvironmentPatch, w as changeSetToGraph, x as createRailwayContext, y as database, z as define, A as defineRailway, F as diffGraphs, H as empty, J as environmentConfigToGraph, K as evaluateRailwayFile, L as evaluateRailwayProject, M as findRailwayFile, N as fn, O as github, Q as graphToEnvironmentConfig, T as group, U as iac, W as image, X as indexGraph, Y as mongo, Z as mysql, _ as postgres, $ as project, a0 as redis, a1 as ref, a2 as renderChangeSet, a3 as renderPatchDiff, a4 as renderRailwayGraphTypes, a5 as resourceAddress, a6 as service, a7 as template, a8 as validateChangeSet, a9 as validateGraph, aa as volume } from './index-DKbAXI_A.js';

interface RailwayGraphQLErrorItem {
    message: string;
    path?: readonly (string | number)[];
    extensions?: Record<string, unknown>;
    [key: string]: unknown;
}
declare class RailwayError extends Error {
    constructor(message: string, options?: ErrorOptions);
}
declare class RailwayAuthError extends RailwayError {
    readonly variable: string;
    constructor(variable: string);
}
/** A WebSocket or network transport failure. */
declare class RailwayConnectionError extends RailwayError {
    readonly closeCode: number | undefined;
    constructor(args: {
        message: string;
        closeCode?: number;
        cause?: unknown;
    });
}
/** Stable code backboard sets in error extensions when an apply's base config is stale. */
declare const STALE_ENVIRONMENT_BASE_CODE = "STALE_ENVIRONMENT_BASE";
/** Thrown by IaC apply when the environment changed since the plan's base snapshot. */
declare class StaleEnvironmentError extends RailwayError {
}
declare class RailwayGraphQLError extends RailwayError {
    readonly status: number;
    readonly errors: readonly RailwayGraphQLErrorItem[];
    readonly responseBody: unknown;
    constructor(args: {
        message: string;
        status: number;
        errors?: readonly RailwayGraphQLErrorItem[];
        responseBody?: unknown;
    });
}

/** Controls a sandbox's access to the environment's private network. */
type SandboxNetworkIsolation$1 = 
/** Peer-isolated with public NAT egress only; no access to the environment's private network. Default. */
'ISOLATED'
/** Joins the environment's private network. */
 | 'PRIVATE';
type SandboxStatus$1 = 'CREATING' | 'DESTROYED' | 'DESTROYING' | 'FAILED' | 'RUNNING';
type RailwaySandboxFieldsFragment = {
    __typename?: 'Sandbox';
    id: string;
    status: SandboxStatus$1;
    networkIsolation: SandboxNetworkIsolation$1;
    environmentId: string;
    region: string;
    idleTimeoutMinutes?: number | null;
    createdAt: string;
};
type RailwaySandboxCheckpointFieldsFragment = {
    __typename?: 'SandboxCheckpoint';
    id: string;
    key: string;
    environmentId: string;
    createdAt: string;
};

type SandboxStatus = RailwaySandboxFieldsFragment["status"];
type SandboxNetworkIsolation = RailwaySandboxFieldsFragment["networkIsolation"];
type SandboxInfo = RailwaySandboxFieldsFragment;
/** Final outcome of an exec; identical shape for short and long-running commands. */
interface ExecResult {
    /** Process exit code; -1 means terminated by a signal (e.g. after `kill()`). */
    exitCode: number | null;
    stdout: string;
    stderr: string;
    /** True when the server cut the output; the streams may be incomplete. */
    truncated: boolean;
    /** True when the client-side `timeoutSec` deadline killed the command. */
    timedOut: boolean;
}
/**
 * A bootable snapshot of a sandbox's disk. `key` is the user-given name for
 * checkpoints captured with `checkpoint`, or the recipe hash for built ones.
 */
type SandboxCheckpointInfo = RailwaySandboxCheckpointFieldsFragment;
/** Knobs shared by every sandbox-creating call: `create`, `create(template)`, and `fork`. */
interface SandboxCreationOptions {
    idleTimeoutMinutes?: number;
    networkIsolation?: SandboxNetworkIsolation;
    /** Runtime env baked into the sandbox, available to every command. Values may use Railway references (e.g. `${{shared.FOO}}`). */
    env?: Record<string, string>;
}
interface CreateOptions extends RailwayClientConfig, SandboxCreationOptions {
    environmentId?: string;
}
type ForkOptions = SandboxCreationOptions;
interface ConnectOptions extends RailwayClientConfig {
    environmentId?: string;
}
interface ListOptions extends RailwayClientConfig {
    environmentId?: string;
    first?: number;
    after?: string;
}
/** Reattach to an exec started earlier (its name comes from `ExecHandle.sessionName`). */
interface ExecReattachTarget {
    sessionName: string;
}
type ExecTarget = string | ExecReattachTarget;
/** Signal names accepted by `ExecHandle.kill()` (sent to the process group). */
type ExecSignal = "HUP" | "INT" | "QUIT" | "KILL" | "TERM";
interface ExecOptions {
    /**
     * Kill the command after this many seconds and resolve with
     * `timedOut: true`. Enforced client-side by closing the exec session.
     */
    timeoutSec?: number;
    /**
     * Working directory for the command (the sandbox default is `/`).
     * The exec fails if the directory does not exist.
     * Fresh execs only — rejected on reattach (`exec({ sessionName })`).
     */
    cwd?: string;
    /**
     * Extra environment variables for this command, layered over the sandbox's
     * own env (these take precedence). Fresh execs only — rejected on reattach
     * (`exec({ sessionName })`).
     */
    env?: Record<string, string>;
    /** Receives each stdout chunk as it arrives. A throw rejects the exec. */
    onStdout?: (chunk: string) => void;
    /** Receives each stderr chunk as it arrives. A throw rejects the exec. */
    onStderr?: (chunk: string) => void;
    /**
     * On reattach (`exec({ sessionName })`), set `true` to resume from the
     * server's last-read cursor — exact, but lossy if a previous reading client
     * didn't keep up before detaching. Defaults to `false`: replay all retained
     * logs for the session (lossless, may repeat output an earlier reader saw).
     * Ignored on a fresh exec.
     */
    resumeFromLastRead?: boolean;
}
/** A directory entry or stat result from the sandbox filesystem. */
interface SandboxFileEntry {
    /** Base name (no directory prefix). */
    name: string;
    /** Size in bytes. */
    size: number;
    /**
     * File mode. The low 9 bits (`mode & 0o777`) are the POSIX permission
     * bits; higher bits encode the file type in a non-POSIX layout, so use
     * `isDir` rather than masking type bits.
     */
    mode: number;
    isDir: boolean;
    /** Modification time, RFC 3339 UTC. */
    modTime: string;
}
type FileReadFormat = "text" | "bytes" | "stream";
interface FileReadOptions {
    /** Start reading at this non-negative byte offset. Mutually exclusive with `fromEnd`. */
    offset?: number;
    /** Read at most this many bytes (to EOF when omitted). Must be positive. */
    length?: number;
    /** Read the last `length` bytes (a tail read). Requires `length`. */
    fromEnd?: boolean;
}
/**
 * Content accepted by `files.write`. Streams and async iterables upload
 * without buffering, so pushes larger than memory are safe, but they are
 * one-shot: a session dropped mid-stream can't be retried. Pass a factory
 * (`() => stream`) when the content can be produced again; the write is then
 * retried automatically like the in-memory forms.
 */
type FileWriteData = string | Uint8Array | ArrayBuffer | Blob | ReadableStream<Uint8Array> | AsyncIterable<Uint8Array> | (() => ReadableStream<Uint8Array> | AsyncIterable<Uint8Array>);
interface FileWriteOptions {
    /**
     * POSIX permission bits for the file (e.g. `0o755`), applied right after
     * the upload completes. Files default to `0644` when omitted.
     */
    mode?: number;
}
interface TemplateBuildOptions extends RailwayClientConfig {
    environmentId?: string;
}
/** Connection/environment options for the checkpoint statics (`Sandbox.checkpoints` etc.). */
interface CheckpointOptions extends RailwayClientConfig {
    environmentId?: string;
}

/** Immutable sandbox base recipe returned by `Sandbox.template()`. */
interface SandboxTemplate {
    /** Add a shell command build step. */
    run(command: string): SandboxTemplate;
    /** Install Debian packages with apt. */
    withPackages(...packages: string[]): SandboxTemplate;
    /**
     * Set build-time environment variables available to the build instructions.
     * Values may use Railway references (e.g. `${{shared.FOO}}`), resolved at
     * build time. Build-time only — runtime env comes from `create({ env })`.
     */
    withEnv(vars: Record<string, string>): SandboxTemplate;
    /** Set the working directory for later build steps. */
    workdir(dir: string): SandboxTemplate;
    /** Build the template before creating sandboxes from it. */
    build(options?: TemplateBuildOptions): Promise<SandboxTemplate>;
}

/**
 * An in-flight exec. Awaiting it (or any Promise method) yields the final
 * `ExecResult`; `sessionName` and `kill()` manage the command while it runs.
 */
declare class ExecHandle implements Promise<ExecResult> {
    #private;
    /** Durable session name for this exec; reattach via `exec({ sessionName })`. */
    readonly sessionName: Promise<string>;
    readonly [Symbol.toStringTag] = "ExecHandle";
    /** Constructed by `Sandbox.exec`; not constructible from outside the SDK. */
    private constructor();
    then<TResult1 = ExecResult, TResult2 = never>(onfulfilled?: ((value: ExecResult) => TResult1 | PromiseLike<TResult1>) | null, onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null): Promise<TResult1 | TResult2>;
    catch<TResult = never>(onrejected?: ((reason: unknown) => TResult | PromiseLike<TResult>) | null): Promise<ExecResult | TResult>;
    finally(onfinally?: (() => void) | null): Promise<ExecResult>;
    /** The final result as a plain promise; identical to awaiting the handle. */
    result(): Promise<ExecResult>;
    /**
     * Terminate the running command with a signal (default `TERM`) delivered to
     * its process group — a real kill, regardless of durable sessions. The handle
     * then settles with the command's exit (a signalled process reports exit code
     * `-1`). To stop streaming WITHOUT ending the command, use `detach()`.
     */
    kill(signal?: ExecSignal): Promise<boolean>;
    /**
     * Stop streaming and close the WebSocket without ending the command — the
     * durable session keeps running server-side. Resolves with its `sessionName`
     * so you can reattach later via `exec({ sessionName })`; rejects if the server
     * assigned no durable session (reattach is impossible). The handle itself
     * settles with the output captured up to the detach.
     */
    detach(): Promise<string>;
}

/**
 * File operations on a live sandbox, exposed as `sandbox.files`. Each
 * operation mints a short-lived `files`-scoped token and opens its own
 * tcp-proxy `/ws/files` session (the bridge serves one request per
 * connection), so concurrent operations run independently.
 *
 * Paths are absolute within the sandbox filesystem. Content streams in
 * 64KB frames both ways: pushes accept streams without buffering, and
 * `read(path, { format: "stream" })` holds at most one transfer segment in
 * memory, so files larger than memory transfer safely.
 */
declare class SandboxFiles {
    #private;
    /** Constructed by `Sandbox#files`; not constructible from outside the SDK. */
    private constructor();
    /** Read a file as text (UTF-8). */
    read(path: string, options?: FileReadOptions & {
        format?: "text";
    }): Promise<string>;
    /** Read a file as raw bytes. */
    read(path: string, options: FileReadOptions & {
        format: "bytes";
    }): Promise<Uint8Array>;
    /**
     * Read a file as a stream of byte chunks. Memory stays bounded regardless
     * of file size: a consumer slower than the network holds back the transfer
     * rather than buffering it. Cancelling the stream aborts the transfer.
     * Errors after the stream is returned (including a missing file) surface
     * through the stream.
     */
    read(path: string, options: FileReadOptions & {
        format: "stream";
    }): Promise<ReadableStream<Uint8Array>>;
    /**
     * Write a file, creating it (and truncating any existing content). Missing
     * parent directories are created automatically. Strings, bytes, blobs, and
     * factory sources upload with retry if the session drops mid-transfer;
     * bare streams and async iterables upload unbuffered but are one-shot, so
     * a session dropped mid-stream surfaces as `RailwayConnectionError` (and a
     * partial file may remain at `path`).
     *
     * Files are created `0644`; pass `mode` to set permissions (applied right
     * after the upload completes).
     */
    write(path: string, data: FileWriteData, options?: FileWriteOptions): Promise<void>;
    /** List a directory (entries carry name, size, mode, and modify time). */
    list(path: string): Promise<SandboxFileEntry[]>;
    /** Stat a path (follows symlinks). Throws `SandboxFileNotFoundError` if absent. */
    stat(path: string): Promise<SandboxFileEntry>;
    /** Whether a path exists (file or directory). */
    exists(path: string): Promise<boolean>;
    /** Create a directory, including missing parents (like `mkdir -p`). */
    mkdir(path: string): Promise<void>;
    /**
     * Remove a file or empty directory. For recursive deletes use
     * `sandbox.exec("rm -rf ...")`.
     */
    remove(path: string): Promise<void>;
    /** Rename (move) a file or directory within the sandbox. */
    rename(oldPath: string, newPath: string): Promise<void>;
}

/**
 * A live Railway sandbox. There is no separate client: a sandbox always comes
 * from somewhere — nothing (`Sandbox.create`), an id (`Sandbox.connect`), a
 * reusable base (`Sandbox.template()`), or a saved checkpoint
 * (`Sandbox.create(name)`). The constructor is private; use the
 * static factories.
 */
declare class Sandbox implements AsyncDisposable {
    #private;
    private constructor();
    get id(): string;
    get status(): SandboxStatus;
    /** Network access mode: `ISOLATED` (NAT egress only) or `PRIVATE` (joins the environment private network). */
    get networkIsolation(): SandboxNetworkIsolation;
    get environmentId(): string;
    get region(): string;
    get idleTimeoutMinutes(): number | null;
    get createdAt(): string;
    /** Return a new immutable sandbox template. */
    static template(): SandboxTemplate;
    /** Boot from a saved checkpoint (one captured with `checkpoint`). */
    static create(checkpointName: string, options?: CreateOptions): Promise<Sandbox>;
    static create(template: SandboxTemplate, options?: CreateOptions): Promise<Sandbox>;
    static create(source: Sandbox, options?: ForkOptions): Promise<Sandbox>;
    static create(options?: CreateOptions): Promise<Sandbox>;
    static connect(id: string, options?: ConnectOptions): Promise<Sandbox>;
    static list(options?: ListOptions): Promise<SandboxInfo[]>;
    /** List the environment's named checkpoints (newest first). */
    static checkpoints(options?: CheckpointOptions): Promise<SandboxCheckpointInfo[]>;
    /** Rename a checkpoint by id; new sandboxes boot from it via the new name. */
    static renameCheckpoint(id: string, name: string, options?: CheckpointOptions): Promise<SandboxCheckpointInfo>;
    /** Delete a checkpoint by id (find it by `key` via `Sandbox.checkpoints`). */
    static deleteCheckpoint(id: string, options?: CheckpointOptions): Promise<void>;
    /**
     * Run a command. Awaiting the handle resolves the final `ExecResult` —
     * short commands return directly, long-running ones transparently stream
     * output (see `ExecOptions.onStdout`/`onStderr`) until the command exits.
     * The handle also exposes `sessionName` and `kill()`. A `timeoutSec` deadline
     * kills the command and resolves with `timedOut: true` rather than
     * rejecting.
     */
    exec(command: string, options?: ExecOptions): ExecHandle;
    /** Reattach to a running exec by session name; resumes the retained output. */
    exec(target: ExecReattachTarget, options?: ExecOptions): ExecHandle;
    /**
     * File operations on this sandbox's filesystem: `read`, `write`, `list`,
     * `stat`, `exists`, `mkdir`, `remove`, `rename`. Reads and writes stream,
     * so files larger than memory transfer safely.
     */
    get files(): SandboxFiles;
    /**
     * Fork this running sandbox into a new, independent one. Clones the
     * filesystem (a fresh boot, not live processes) into the same environment.
     */
    fork(options?: ForkOptions): Promise<Sandbox>;
    /**
     * Capture this sandbox's current disk into a reusable named checkpoint,
     * bootable as soon as this resolves. The sandbox must be running and the
     * name unused. Boot new sandboxes from it with `Sandbox.create(name)`.
     */
    checkpoint(name: string): Promise<SandboxCheckpointInfo>;
    destroy(): Promise<void>;
    /** Re-reads the sandbox to refresh `status` and other fields in place. */
    refresh(): Promise<this>;
    [Symbol.asyncDispose](): Promise<void>;
    toJSON(): SandboxInfo;
}

declare class SandboxNotFoundError extends RailwayError {
    readonly id: string;
    readonly environmentId: string;
    constructor(args: {
        id: string;
        environmentId: string;
    });
}
/** A sandbox reached FAILED/DESTROYING/DESTROYED before becoming RUNNING. */
declare class SandboxFailedError extends RailwayError {
    readonly id: string;
    readonly status: SandboxStatus;
    constructor(args: {
        id: string;
        status: SandboxStatus;
    });
}
/** A server-side template build finished in the FAILED state. */
declare class SandboxTemplateBuildError extends RailwayError {
    readonly templateId: string;
    readonly environmentId: string;
    constructor(args: {
        templateId: string;
        environmentId: string;
    });
}
/** A file operation was rejected by the sandbox (the message is the VM's error text). */
declare class SandboxFilesError extends RailwayError {
    readonly operation: string;
    readonly path: string;
    constructor(args: {
        operation: string;
        path: string;
        message: string;
    });
}
/** The path does not exist in the sandbox. */
declare class SandboxFileNotFoundError extends SandboxFilesError {
}
/** A readiness wait timed out. */
declare class SandboxTimeoutError extends RailwayError {
    readonly resource: "sandbox" | "template";
    readonly id: string;
    readonly lastStatus: string;
    readonly timeoutMs: number;
    constructor(args: {
        resource: "sandbox" | "template";
        id: string;
        lastStatus: string;
        timeoutMs: number;
    });
}

export { type CheckpointOptions, type ConnectOptions, type CreateOptions, ExecHandle, type ExecOptions, type ExecReattachTarget, type ExecResult, type ExecSignal, type ExecTarget, type FileReadFormat, type FileReadOptions, type FileWriteData, type FileWriteOptions, type ForkOptions, type ListOptions, RailwayAuthError, RailwayClientConfig, RailwayConnectionError, RailwayError, RailwayGraphQLError, type RailwayGraphQLErrorItem, STALE_ENVIRONMENT_BASE_CODE, Sandbox, type SandboxCheckpointInfo, SandboxFailedError, type SandboxFileEntry, SandboxFileNotFoundError, SandboxFiles, SandboxFilesError, type SandboxInfo, type SandboxNetworkIsolation, SandboxNotFoundError, type SandboxStatus, type SandboxTemplate, SandboxTemplateBuildError, SandboxTimeoutError, StaleEnvironmentError, type TemplateBuildOptions };
